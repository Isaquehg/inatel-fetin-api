package com.innov8ors.backend;

import static org.junit.jupiter.api.Assertions.*;
// import com.innov8ors.backend.model.Grade;

import org.junit.jupiter.api.Test;

/* 
public class GradeTest {

    @Test
    public void testConstructorWithGrade() {
        Grade grade = new Grade(80);
        assertEquals(80, grade.getGrade());
        assertNull(grade.getTeam_id());
        assertNull(grade.getTeam_id());
        assertNull(grade.getPhase_id());
    }

    @Test
    public void testConstructorWithoutGrade() {
        Grade grade = new Grade();
        assertEquals(0, grade.getGrade());
        assertNull(grade.getTeam_id());
        assertNull(grade.getTeam_id());
        assertNull(grade.getPhase_id());
    }

    @Test
    public void testSettersAndGetters() {
        Grade grade = new Grade();
        grade.setId(1L);
        grade.setTeam_id(2L);
        grade.setPhase_id(3L);
        grade.setGrade(90);

        assertEquals(1L, grade.getId());
        assertEquals(2L, grade.getTeam_id());
        assertEquals(3L, grade.getPhase_id());
        assertEquals(90, grade.getGrade());
    }
}
*/